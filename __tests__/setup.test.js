describe('Ambiente de Testes', () => {
  it('Deve garantir que o Jest está configurado e a matemática básica funciona', () => {
    const soma = 2 + 2;
    expect(soma).toBe(4); // A nossa expectativa é que seja 4
  });
});